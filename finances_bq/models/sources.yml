version: 2

sources:
  - name: finances_raw
    schema: finanzas_personales_mds
    database: big-query-406221
    tables: 
      - name: raw_transactions
        config:
          freshness:
            warn_after:
              count: 30
              period: day
          loaded_at_field: "CAST(segun_un_periodo AS TIMESTAMP)"
      - name: raw_transactions_metadata
        config:
          freshness:
            error_after:
              count: 24
              period: hour
          loaded_at_field: updated
      - name: claves
      - name: presupuesto
      - name: hip_depa_alameda_dolores